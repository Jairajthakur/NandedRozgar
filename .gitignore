# ── Secrets — never commit real env files ─────────────────────────────────────
.env.local
.env.production
.env.*.local

# ── Firebase / Google — contains sensitive API keys ───────────────────────────
# Use EAS secrets or environment variables instead.
# The checked-in google-services.json is a placeholder template only.
# Your REAL google-services.json must never be committed.
google-services.json
GoogleService-Info.plist

# ── Node ──────────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Expo / React Native ───────────────────────────────────────────────────────
.expo/
dist/
web-build/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*

# ── Build outputs ─────────────────────────────────────────────────────────────
android/
ios/
*.apk
*.aab
*.ipa

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
